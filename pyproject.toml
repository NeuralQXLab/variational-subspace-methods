[project]
name = "subspace"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "bridge",
    "matplotlib>=3.10.3",
    "qutip>=5.1.1",
]

[tool.uv.sources]
bridge = { path = "libraries/bridge", editable = true }